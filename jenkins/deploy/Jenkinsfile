pipeline {

    agent any

    tools {
	'org.jenkinsci.plugins.docker.commons.tools.DockerTool' '18.09'
    }

    stages {
        stage('Install dependecies') {
            steps {
                echo "hola mundo"
		sh "docker -v"
            }
        }

    }
}